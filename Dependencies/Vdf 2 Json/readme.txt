Install python

Through command prompt

Install PyInstaller
	pip install pyinstaller
	
Install Auto PY to EXE
	pip install auto-py-to-exe
	
Use the gui 'auto-py-to-exe' in cmd to compile to one directory because single executable doesnt work for some reason
	..\vdf-3.4\vdf2json\vdf2json\cli.py